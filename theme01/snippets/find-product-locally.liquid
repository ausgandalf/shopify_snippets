<script id='lcly-script-0' async></script>
<script id='lcly-config-0'>
  const isElementLoaded = async (selector) => {
    while (document.querySelector(selector) === null) {
      await new Promise((resolve) => requestAnimationFrame(resolve));
    }
    return document.querySelector(selector);
  };

  isElementLoaded('#lcly-button-0').then(() => {
    var __lcly_channel_domain_0 = 'locally';
    var lcly_config_0 = {
      company_name: 'Whitelabel',
      button_text: 'FIND IN STORE',
      button_id: 'HTML',
      company_id: '201753',
      css: '4',
      color_0: '#241E18',
      color_1: '#241E18',
      show_location_switcher: '1',
      show_location_prompt: '1',
      lang: 'en-us',
      show_dealers: '1',
      n_dealers: '3',
      show_address: '1',
      show_phone: '1',
    };
    var lcly_query_0 = Object.keys(lcly_config_0)
      .reduce(function (a, k) {
        a.push(
          encodeURIComponent(k) +
            '=' +
            encodeURIComponent(
              typeof lcly_config_0[k] === 'object' && lcly_config_0[k] !== null ? JSON.stringify(lcly_config_0[k]) : lcly_config_0[k],
            ),
        );
        return a;
      }, [])
      .join('&');
    var lcly_endpoint_0 = 'https://whitelabel.locally.com/stores/map.js?' + lcly_query_0;
    document.getElementById('lcly-script-0').src = lcly_endpoint_0;
  });
</script>
